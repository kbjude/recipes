import React from 'react';
import { BrowserRouter, Switch, Route } from 'react-router-dom';

const routes = () => {
    <div>
        <BrowserRouter>
            <Switch>

            </Switch>
        </BrowserRouter>
    </div>
}