import './App.css';
import { Provider } from 'react-redux';

function App() {
  const [recipes, setRecipes] = useState(initialState);
  return (
    <div className="App">
      Capstone
    </div>
  );
}

export default App;
